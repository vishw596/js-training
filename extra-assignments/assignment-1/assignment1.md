# JavaScript MCQs & Exercises

---

## 🔹 MCQs (Choose the correct option)

### 1. Which keyword is used to stop a loop immediately?
- a) stop  
- b) **break** ✅  
- c) exit  
- d) pause  

---

### 2. What does `split()` return?
- a) String  
- b) Object  
- c) **Array** ✅  
- d) Number  

---

### 3. Which loop is best when the number of iterations is known?
- a) while  
- b) do-while  
- c) **for** ✅  
- d) forEach  

---

### 4. What will `["a","b","c"].join("-")` return?
- a) `"abc"`  
- b) `"a,b,c"`  
- c) **"a-b-c"** ✅  
- d) `["a-b-c"]`  

---

### 5. Which method converts array elements into a single value?
- a) map  
- b) filter  
- c) **reduce** ✅  
- d) forEach  

---

### 6. Which loop works directly on array elements?
- a) for  
- b) for...in  
- c) **forEach** ✅  
- d) while  

> ❗ **Note:** `forEach` works directly on array elements, not indexes.

---

### 7. What does `this` refer to inside an object method?
- a) global object  
- b) function itself  
- c) **object that owns the method** ✅  
- d) undefined  

---

### 8. Which operator is used to copy array values?
- a) &  
- b) **... (spread operator)** ✅  
- c) ||  
- d) ??  

---

### 9. What does `filter()` return?
- a) Number  
- b) Boolean  
- c) Object  
- d) **Array** ✅  

---

### 10. Which method changes the original array?
- a) map  
- b) filter  
- c) reduce  
- d) forEach  

✅ **Correct Answer:** **None of the above**

> All listed methods return new values or perform iteration without mutating the array itself.

---

### 11. What is a shallow copy?
- a) Copy without reference  
- b) **Copy with reference** ✅  
- c) Deep nested copy  
- d) JSON copy  

---

### 12. Which loop is used mostly for objects?
- a) for  
- b) forEach  
- c) for...of  
- d) **for...in** ✅  

---

### 13. What does `map()` return?
- a) Modified array  
- b) **New array** ✅  
- c) Object  
- d) Boolean  

---

### 14. Which is NOT a control flow statement?
- a) if  
- b) switch  
- c) loop  
- d) **join** ✅  

---

### 15. What is the output type of `reduce()`?
- a) Always array  
- b) Always object  
- c) **Depends on logic** ✅  
- d) Always number  

---

## 🔹 Exercises

### 1. Even or Odd Numbers
Write a program to print numbers from **1 to 50** and show whether each number is **Even** or **Odd**.

---

### 2. String Transformation Function
Create a function that takes a string and:
- Converts it to lowercase  
- Splits it by space  
- Joins it using `_`  

---

### 3. Multiply Array Elements
Given an array of numbers, use `map()` to multiply each number by **3**.

---

### 4. Filter Adult Ages
Given an array of ages, use `filter()` to return only ages **18 and above**.

---

### 5. Object Method Using `this`
Create an object `user` with a method that prints:

